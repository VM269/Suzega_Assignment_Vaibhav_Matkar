apiVersion: v1
kind: Service
metadata:
  name: inference-service
  namespace: inference
spec:
  type: ClusterIP
  selector:
    app: inference-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
